authenticate:
  handler: ./dist/user/authenticate/index.handler
  package:
    patterns:
      - "dist/user/authenticate/**"
      - "!dist/user/authenticate/**/*.ts"
      - "!dist/user/authenticate/**/*.yml"
  name: authenticate-${self:provider.stage}
  memorySize: 128 # mb
  timeout: 5 # seconds
  events:
    - http:
        path: user/authenticate
        method: POST
        cors: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - "cognito-idp:InitiateAuth"
      Resource: !GetAtt CognitoUserPool.Arn
