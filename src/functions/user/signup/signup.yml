signup:
  handler: ./dist/user/signup/index.handler
  package:
    patterns:
      - "dist/user/signup/**"
      - "!dist/user/signup/**/*.ts"
      - "!dist/user/signup/**/*.yml"
  name: signup-${self:provider.stage}
  memorySize: 128 # mb
  timeout: 5 # seconds
  events:
    - http:
        path: user/signup
        method: POST
        cors: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - "dynamodb:PutItem"
      Resource: !GetAtt UsersTable.Arn
    - Effect: Allow
      Action:
        - "cognito-idp:SignUp"
      Resource: !GetAtt CognitoUserPool.Arn
