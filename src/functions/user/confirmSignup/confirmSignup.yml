confirmSignup:
  handler: ./dist/user/confirmSignup/index.handler
  package:
    patterns:
      - "dist/user/confirmSignup/**"
      - "!dist/user/confirmSignup/**/*.ts"
      - "!dist/user/confirmSignup/**/*.yml"
  name: confirm-signup-${self:provider.stage}
  memorySize: 128 # mb
  timeout: 5 # seconds
  events:
    - http:
        path: user/confirmSignup
        method: POST
        cors: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - "cognito-idp:ConfirmSignUp"
      Resource: !GetAtt CognitoUserPool.Arn
