resendCode:
  handler: ./dist/user/resendCode/index.handler
  package:
    patterns:
      - "dist/user/resendCode/**"
      - "!dist/user/resendCode/**/*.ts"
      - "!dist/user/resendCode/**/*.yml"
  name: resend-code-${self:provider.stage}
  memorySize: 128 # mb
  timeout: 5 # seconds
  events:
    - http:
        path: user/resendCode
        method: POST
        cors: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - "cognito-idp:ResendConfirmationCode"
      Resource: !GetAtt CognitoUserPool.Arn
