postConfirmation:
  handler: ./dist/auth/postConfirmation/index.handler
  package:
    patterns:
      - "dist/auth/postConfirmation/**"
      - "!dist/auth/postConfirmation/**/*.ts"
      - "!dist/auth/postConfirmation/**/*.yml"
  name: post-confirmation-${self:provider.stage}
  memorySize: 128 # mb
  timeout: 5 # seconds
  events:
    - cognitoUserPool:
        pool: ${self:custom.userPoolName}
        trigger: PostConfirmation
        existing: true
        forceDeploy: true
  iamRoleStatements:
    - Effect: Allow
      Action:
        - "dynamodb:UpdateItem"
      Resource: !GetAtt UsersTable.Arn
